# ========== DATABASE CONFIGURATION ==========
spring.datasource.url=jdbc:postgresql://aws-0-eu-central-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.nxskejvizzgohrggpjsn
spring.datasource.password=CampusConnect@290
# spring.datasource.url=jdbc:postgresql://localhost:5432/CampusConnect
# spring.datasource.username=postgres
# spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# ========== JPA CONFIGURATION ==========
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true  
# Set to true to log SQL queries
spring.jpa.properties.hibernate.format_sql=true

# ========== SERVER CONFIGURATION ==========
server.port=8080
spring.application.name=campus-connect

# ========== OPTIONAL: LOGGING CONFIGURATION ==========
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT secret key
# security.jwt.secret-key =${JWT_SECRET_KEY}
# security.jwt.expiration-time=3600000

# Security
spring.security.user.name=admin
spring.security.user.password=adminpass

# Mail properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=khillo2001@gmail.com

# spring.mail.password=${APP_PASSWORD}
spring.mail.password=prew hicl sthd ruag
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true